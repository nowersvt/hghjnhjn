version: 3

endpoints:
  - endpoint: "/swagger"
    method: GET
    output_encoding: json
    backend:
      - url_pattern: "/"
        extra_config:
          github.com/devopsfaith/krakend-urlrewrite:
            patterns:
              - match: "/swagger(.*)"
                replace: "${1}"
                name: "swagger_rewrite"
